apiVersion: v1
kind: Service
metadata:
  name: clutchzone-backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: clutchzone-backend
  ports:
    - name: http
      port: 80
      targetPort: http
