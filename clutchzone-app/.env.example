# URL pública do backend (repositório separado clutchzone-backend), sem barra final.
# Obrigatória em builds de produção (`npm run build`) — sem ela, o app fica sem backend
# configurado fora de localhost. Em dev, cai automaticamente em http://localhost:3001.
VITE_BACKEND_URL=http://localhost:3001
