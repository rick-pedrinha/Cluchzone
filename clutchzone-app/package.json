{
  "name": "clutchzone-app",
  "private": true,
  "version": "2.0.0",
  "description": "CLUCHZONE — Professional eSports Tournament Platform",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts --report-unused-disable-directives --max-warnings 0"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.3",
    "vite": "8.1.4"
  }
}
